---
description: "Use when writing Python code. Enforces ASCII-only stdout output, avoiding Unicode characters to avoid encoding issues especially on Windows."
applyTo: "**/*.py"
---
- Use standard ASCII for all stdout output; avoid Unicode characters
